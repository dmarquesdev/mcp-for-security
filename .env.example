# ============================================================
# MCP Security Tools - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values.
# All variables are optional unless marked [REQUIRED].
#
# Loading priority:
#   1. Real environment variables (always win)
#   2. Values from .env file
#
# To use a custom .env path, set MCP_ENV_FILE=/path/to/.env
# ============================================================

# --- Gateway (Nginx) ---
# [RECOMMENDED] API key for authenticating requests to the gateway.
# All tool endpoints require X-API-Key header. /healthz and /services are open.
# MCP_API_KEY=your_secure_api_key_here
# GATEWAY_PORT=8000

# --- MobSF (Mobile Security Framework) ---
# [REQUIRED for mobsf-mcp] MobSF server URL and API key
# MOBSF_BASE_URL=http://localhost:8000
# MOBSF_API_KEY=your_mobsf_api_key

# --- AWS / ScoutSuite (Cloud Security Auditing) ---
# [REQUIRED for scoutsuite-mcp] AWS credentials
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_SESSION_TOKEN=your_session_token

# --- GitHub Subdomains ---
# [REQUIRED for github-subdomains-mcp] GitHub API token
# GITHUB_TOKEN=ghp_your_github_token

# --- WPScan (WordPress Security Scanner) ---
# [OPTIONAL] Enables vulnerability database lookups (free: 25 req/day)
# Register at https://wpscan.com/api
# WPSCAN_API_TOKEN=your_wpscan_token

# --- Assetfinder (Subdomain Discovery) ---
# [OPTIONAL] Additional data sources for subdomain enumeration
# FB_APP_ID=your_facebook_app_id
# FB_APP_SECRET=your_facebook_app_secret
# VT_API_KEY=your_virustotal_api_key
# SPYSE_API_TOKEN=your_spyse_api_token

# --- Nuclei (Vulnerability Scanner) ---
# [OPTIONAL] Interactsh token for out-of-band testing
# INTERACTSH_TOKEN=your_interactsh_token

# --- Subfinder (Subdomain Enumeration) ---
# [OPTIONAL] Path to provider config with API keys for 20+ sources
# Default: ~/.config/subfinder/provider-config.yaml
# See: https://github.com/projectdiscovery/subfinder#post-installation-instructions
# SUBFINDER_CONFIG_FILE=/path/to/provider-config.yaml

# --- Amass (Attack Surface Mapping) ---
# [OPTIONAL] Path to data sources config with API keys
# Default: ~/.config/amass/datasources.yaml
# See: https://github.com/owasp-amass/amass/blob/master/doc/user_guide.md
# AMASS_CONFIG_FILE=/path/to/datasources.yaml

# --- asnmap (ASN to CIDR Mapping) ---
# [OPTIONAL] ProjectDiscovery Cloud Platform API key for enhanced data
# Free API key: https://cloud.projectdiscovery.io/
# PDCP_API_KEY=pdcp_your_api_key_here

# --- E2E Test Targets ---
# Credentials for E2E test databases. Defaults are fine for local testing.
# DVWA_DB_ROOT_PASSWORD=dvwa
# DVWA_DB_PASSWORD=p@ssw0rd
# WORDPRESS_DB_ROOT_PASSWORD=wp
# WORDPRESS_DB_PASSWORD=wp
# HTTPBIN_PORT=8081
# DVWA_PORT=8082
# WORDPRESS_PORT=8083
