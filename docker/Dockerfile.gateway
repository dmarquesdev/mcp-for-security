FROM nginx:alpine
RUN apk add --no-cache curl
COPY docker/nginx.conf /etc/nginx/nginx.conf
COPY docker/services.json /etc/nginx/services.json
EXPOSE 8000
HEALTHCHECK --interval=15s --timeout=3s \
    CMD curl -sf http://localhost:8000/healthz || exit 1
